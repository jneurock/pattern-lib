<section class="row">
  <div class="large-8 columns">
    <h1>Healthsparq Pattern Library</h1>

    {{render "atoms.index"}}

    {{render "molecules.index"}}

    {{render "organisms.index"}}

    {{render "templates.index"}}
  </div>
</section>